import type { Metadata } from "next";
import { AppShell } from "@/components/layout/app-shell";
export const metadata:Metadata={title:"Features",description:"Explore Squad Planner features for cricket auctions, squads, fixtures, player opportunities and volunteer duties.",alternates:{canonical:"/features"}};
const groups=[["Auction planning","Import player pools, create team-specific buckets, assign private priorities and compare Plan A, B and C."],["Live auction","Track purchases, budgets and bucket targets with atomic history, conflict protection and realtime updates."],["Tournament management","Create fixtures, select flexible match squads, record scores and monitor opportunities."],["Team coordination","Assign volunteer duties fairly and review combined contribution across the tournament."]];
export default function Features(){return <AppShell><div className="mx-auto max-w-7xl px-4 py-14 sm:px-6 lg:px-8"><p className="text-sm font-black uppercase tracking-[.16em] text-pitch">Features</p><h1 className="mt-3 max-w-3xl text-4xl font-black tracking-[-.045em] sm:text-5xl">Everything your cricket team needs to plan with confidence.</h1><p className="mt-5 max-w-2xl text-lg leading-8 text-slate-600">A focused toolkit from pre-auction preparation through tournament delivery.</p><div className="mt-10 grid gap-5 md:grid-cols-2">{groups.map(([title,copy])=><article key={title} className="rounded-2xl border bg-white p-6 shadow-sm"><h2 className="text-xl font-bold">{title}</h2><p className="mt-3 leading-7 text-slate-600">{copy}</p></article>)}</div></div></AppShell>}
