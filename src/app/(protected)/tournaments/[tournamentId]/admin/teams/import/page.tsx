import {TeamImport} from "@/components/tournament-admin/team-import";import {PageHeader} from "@/components/ui/page-header";import {requireTournamentAdmin} from "@/lib/tournament/admin-access";
export default async function Page({params}:{params:Promise<{tournamentId:string}>}){const{tournamentId}=await params;await requireTournamentAdmin(tournamentId);return <main className="mx-auto max-w-5xl space-y-6 p-4 sm:p-6 lg:p-8"><PageHeader title="Import tournament teams" description="Preview an XLSX or CSV schedule of teams before creating records and captain invitations."/><TeamImport tournamentId={tournamentId}/></main>}
